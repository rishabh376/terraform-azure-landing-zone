groups:
- name: terraform-azure-landing-zone
  rules:
  - alert: HighErrorRate
    expr: rate(requests_total[5m]) > 5
    for: 5m
    labels: {severity: warning}
    annotations: {summary: "High request rate on terraform-azure-landing-zone"}
